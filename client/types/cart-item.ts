type CartItem = {
    id: string,
    name: string,
    image: string,
    brand: string,
    quantity: number,
    price: number,
    gripSize?: string
    stringOption?: string
}
export default CartItem
